/*global _, Backbone*/
define([
		'underscore',
		'backbone'
	],
	function(UnderscoreLib, BackboneLib) {
		"use strict";
		
		return Backbone.Collection.extend({
			initialize : function() {
				
			}
		});
	}
);
